# Daily report for 2026-04-06

## ~/apps/Termsy

Worked on:
- Inspector and overlay behavior
- Terminal resize / scroll stability
- Font source filtering and platform compatibility
- Session picker navigation and native key affordances

Accomplished:
- Reworked the inspector into a custom trailing overlay panel instead of resizing the terminal[^1]
- Removed the dark dimming overlay from the custom panel[^2]
- Fixed tmux scroll-jitter by suppressing unnecessary SSH window-change forwarding[^3]
- Added keyboard navigation to the new-tab session picker, including arrow keys, Return, and Esc[^4]
- Addressed macOS SwiftUI availability issues with a shared compatibility helper[^5]

LLM misses:
- Assumed session identity should use a UUID-like key, but the user wanted a natural composite key[^6][^7]
  Lesson: When the user proposes a simpler domain key, prefer it over inventing a surrogate identifier unless there is a clear technical need
- Introduced a dark overlay while implementing the inspector panel, but the user wanted the panel without dimming[^8][^9]
  Lesson: Match UI changes to the exact visual requirement; avoid adding default modal-style chrome unless requested

## ~/apps/pimux2000

Worked on:
- Refactoring and simplifying the iOS session view
- Renaming the server binary from `pimux-server` to `pimux
- iPad screenshot generation and dark-mode screenshot behavior
- iOS app polish and sidebar interaction fixes
- Adding a `pimux server status` command

Accomplished:
- Reduced `PiSessionView.swift` substantially by extracting dialog logic into focused files[^10][^11]
- Completed the binary rename migration to `pimux` across build and release configuration[^12]
- Fixed the iPad screenshot capture flow so screenshots are no longer portrait/cropped in the simulator path[^13]
- Completed an iOS sidebar fix and verified the test suite passed cleanly[^14][^15]

LLM misses:
- Assumed screenshot example-data changes should be made in simulator fixtures, but the user meant data from the local computer[^16][^17][^18]
  Lesson: Confirm the target environment before proposing fixture or simulator-based changes

## ~/apps/pirot

Worked on:
- Refining `/convo` behavior in the Pi extension
- Clarifying how extension-authored guidance is represented in chat
- Considering LSP support and why it may not be taking effect

Accomplished:
- Removed the automatic jump into convo mode and replaced it with a UI hint to use `/convo` manually[^19]
- Switched `/convo` steering messages from user-visible messages to custom internal directives, making them distinguishable from real user input[^20]


## ~/apps/talk

Worked on:
- Homepage content expansion for new language examples
- Implementing and stabilizing `let else
- Fixing scope/capture behavior in name resolution
- Making the `let else` example runnable in the site content

Accomplished:
- Added multiple examples to `www/content/index.md`, including closures, trailing block callbacks, custom initializers, `if let`, `let else`, structural matching, imports/exports, and a tiny HTTP server[^21]
- Fixed the underlying `let else`-related crash by adjusting scope capture handling in `src/name_resolution/name_resolver.rs`[^22]
- Restored the `let else` block in `www/content/index.md` to runnable form and verified the site rebuild and example execution[^23]


## ~/apps/sshtestin

Worked on:
- Defining how the Rust crate should run on a host and handle SSH connections with a TUI
- Choosing an SSH server port for development and deployment

Accomplished:
- Confirmed that port 22 is acceptable for the SSH server, with 2222 only suggested as a safer development default[^24]


## ~/apps/wink

Worked on:
- LED matrix layout and wiring on the XIAO ESP32C6
- 2D lighting effects for a 10x6 / 6x10 RGB grid
- Calibration and effect tuning for the panel mapping
- Locking the display to a scanner animation

Accomplished:
- Added a 2D grid effect engine with multiple lighting effects for the 60-LED panel[^25]
- Corrected the likely panel mapping to 6x10 with serpentine columns and a top-left origin[^26]
- Simplified calibration mode to make layout evaluation easier[^27]
- Locked the active display to the scanner effect and removed effect cycling[^28]

LLM misses:
- The calibration mapping was simplified incorrectly by assuming a snake-back layout when the user clarified it should continue in the same direction[^29][^30][^31]
  Lesson: When the user corrects a layout detail, re-evaluate the mapping immediately instead of reinforcing the prior assumption

## ~/apps/pimux2000/pimux-server

Worked on:
- Added optional Postgres backup support for the server’s incoming agent messages
- Adjusted the database design to use a messages table plus a sessions table
- Added a CLI backfill mode for loading historical transcripts
- Updated the transcript-fetch path to accept a host parameter for backfill requests

Accomplished:
- Implemented optional Postgres backup writing with automatic schema creation and reconnect handling[^32]
- Reworked the storage model to use separate sessions and messages tables, removing the earlier single backup table approach[^33]
- Added a host parameter to transcript retrieval and wired the backfill command to use it[^34]


## ~/apps/seekwel

Worked on:
- Designing safer schema changes for seekwel by comparing live SQLite schema against model definitions and reconciling differences
- Refactoring seekwel-macros to separate macro entrypoints, model wiring, analysis, and code generation
- Planning FTS indexing support using `FTS<...>` field syntax in `#[seekwel::model]`
- Discussing the model-definition surface, including removing user-facing `#[derive(Model)]` in favor of `#[seekwel::model]`
- Brainstorming transaction support and related callback/hook design for the library

Accomplished:
- Completed a macro refactor that split `seekwel-macros` into phased modules for entrypoints, wiring, analysis, and code generation[^35]
- Resolved the schema-safety plan by explicitly settling the remaining ambiguity around how v1 identifies seekwel-managed live tables[^36][^37]
- Closed the FTS design plan enough to proceed with a documented approach for model field syntax and indexing behavior[^38]

LLM misses:
- Kept treating the work as an implementation task when the user wanted continued planning and a design document, not code changes[^39][^40]
  Lesson: When the user says to stay in convo mode and keep planning, stay strictly in design/discovery framing and avoid drifting into implementation-oriented language
- Initially suggested asking more questions instead of directly using the requested questionnaire workflow[^41]
  Lesson: If the user explicitly requests convo_questionnaire, follow that mechanism immediately rather than paraphrasing the need for more clarification

[^1]: Yep. I stopped fighting the resize behavior and removed the real inspector. What I changed: - `ContentView.swi…
[^2]: Done — removed the dark overlay
[^3]: I made a surgical fix for this
[^4]: I added keyboard navigation to the new-tab session picker
[^5]: fair. This was turning into whack-a-mole, so I fixed the **whole class** of SwiftUI platform-availability issu…
[^6]: LLM: Yep. For Termsy, the clean version is: - **keep GRDB as the local store** - **sync only session profile record…
[^7]: Correction: why not just use username/hostname/tmuxsession as the key instead of uuid
[^8]: LLM: Yep. I stopped fighting the resize behavior and removed the real inspector. What I changed: - `ContentView.swi…
[^9]: Correction: we don't need the dark overlay
[^10]: No explicit file references — the project uses automatic file scanning
[^11]: **PiSessionView.swift: 1,669 → 1,086 lines** (35% reduction)
[^12]: ### What changed - `pimux-server/Cargo.toml` - added an explicit bin target named `pimux` - set `default-run =…
[^13]: You're right — the previous version was still effectively using the **portrait XCTest attachment path** for iPad
[^14]: All tests pass, no failures, no warnings
[^15]: 54 tests passed, zero failures, zero warnings. Let me check the final line counts
[^16]: LLM: Yes. The clean way is to add it in the deterministic screenshot fixtures: - `pimux2000/Models/UITestFixtures.s…
[^17]: Correction: i mean on this computer. not in the simulator
[^18]: can we add more example data to the screenshots
[^19]: **No more automatic jump into convo mode** - removed the `agent_end` path that auto-switched into convo - now …
[^20]: Added a custom message type: - `convo-directive` - Added …
[^21]: closures + captured state - trailing block callbacks - custom initializers - `if let` - `let else` - structura…
[^22]: added `capture_boundary` to `Scope` - capture lookup now only records captures across real capture boundaries …
[^23]: site rebuilds ✅ - the `let else` example runs ✅ Verified with: ```talk enum Maybe<T> { case some(T) case none …
[^24]: You can use **22**. I only said **2222** because it’s the safer **development** choice. ### Why I suggested 22…
[^25]: Added a grid animator with 6 effects: - diagonal rainbow - plasma - twinkle - ripple - scanner - aurora - Uses…
[^26]: grid size: `6 x 10` - mapping: **serpentine columns** instead of serpentine rows - origin default: **top-left*…
[^27]: no more cycling through layout guesses - no bars - no multiple colors/patterns at once - lower brightness What…
[^28]: fixed active effect to `EffectKind::Scanner` - removed effect cycling logic Verified: - `cargo check` passes I…
[^29]: LLM: no more cycling through layout guesses - no bars - no multiple colors/patterns at once - lower brightness What…
[^30]: Correction: no wait, 3 is continue same direction
[^31]: 1. top left, 2. across a row 3. snake back the other way
[^32]: new optional Postgres backup writer - auto-creates `agent_message_backups` - retries on reconnect if Postgres …
[^33]: changed it. Now: - no raw `agent_message_backups` table - Postgres schema is: - `sessions` - `messages` What i…
[^34]: pimux server backfill` now uses that param when fetching transcripts - host-scoped requests bypass a cached tr…
[^35]: ### Macro refactor Split `seekwel-macros` into phases: - `seekwel-macros/src/lib.rs` - now just the proc-macro…
[^36]: Understood. I’m resolving the remaining implicit gap explicitly: **how v1 knows which live tables are actually…
[^37]: Resolved with the latest answers plus one explicit safety assumption for the remaining type-comparison gap
[^38]: ## Goal Produce a **design document** for adding **FTS indexing** to `seekwel`, using model field syntax like:…
[^39]: Keep planning. User direction: we should kill user-facing #[derive(Model)] during this Keep planning this. Sta…
[^40]: Keep planning. User direction: the final endpoint of this plan is a document. not writing code. You are still …
[^41]: Asked the next two highest-value clarification questions: 1
