# Report for 2026-04-02

# Daily report for 2026-04-02

## ~/apps/pimux2000

Worked on:
- iOS slash commands and command-aware message submission
- iOS session view performance and message rendering cleanup
- Startup/deferred startup logic simplification
- Server/client message ID handling and sync behavior
- iOS scrolling and message arrival behavior

Accomplished:
- Implemented the real fix so slash commands use Pi’s command-aware submission path instead of being sent as normal user messages[^1][^2]
- Removed the deferred startup generation/request-ID approach and moved startup work into a low-priority task[^3][^4]
- Cleaned up the syncer/state flow by persisting streamed messages and updating contents instead of tracking streamedMessages in PiSessionView[^5][^6]
- Confirmed the codebase was in a passing state after the changes, with all builds and tests passing[^7][^8]
- Updated the server/client message ID path so server messages use stable IDs and the Swift client handles them cleanly as 64-bit values[^9][^10]

LLM misses:
- Assumed the slash-command issue was just message submission plumbing, but the iOS behavior still failed to execute `/todo` after the first fix[^11][^12][^13]
  Lesson: Verify the end-to-end command path in the target client before concluding the submission fix is complete
- Pushed toward a more complex client architecture for server client ownership when the user wanted a simpler environment-key approach[^14][^15]
  Lesson: Prefer the simplest requested integration pattern unless there is a clear technical constraint that requires extra indirection

## ~/apps/pirot

Worked on:
- Added delete support for the `/todo` list picker with confirmation behavior
- Reworked planning mode controls, including agent-triggered exit and a Shift-Tab toggle
- Investigated why markdown handoff files were appearing in repo roots
- Adjusted keybinding behavior so Shift-Tab toggles plan mode instead of cycling thinking level

Accomplished:
- Implemented `/todo` delete support by interleaving delete options in the select list and mapping delete selections back to items[^16]
- Enabled planning mode to be exited by the agent through a new `request_planning_mode_exit` tool[^17]
- Changed planning mode interaction so Shift-Tab toggles planning mode on and off, with the built-in thinking-cycle binding removed[^18]
- Confirmed the repo-root markdown files were introduced by a normal pi work session, not by compaction[^19]

LLM misses:
- Initially steered the `/todo` delete feature toward a custom UI approach, but the user had already ruled that out[^20][^21]
  Lesson: When the user constrains implementation options, keep the plan within those constraints and re-check before proposing a design
- Initially treated planning mode as something the agent could not change, then the user requested agent-controlled exit[^22][^23]
  Lesson: If a system restriction may need product-level support, surface it clearly and adapt the plan rather than assuming it is fixed

## ~/apps/seekwel

Worked on:
- Query API behavior and iterator shapes
- Model macro column enums and column-based methods
- Persisted model save/update naming and docs alignment
- README and prelude/API consistency
- Ordering API design for richer Into<Order> inputs

Accomplished:
- Moved builder state bookkeeping into the main crate with new Required/Optional builder helpers[^24]
- Split the query implementation into separate shared, expression, eager, and lazy-related modules[^25]
- Implemented generic wrapper-style query modifiers without boxing for query iterators[^26]
- Generated a sibling `<ModelName>Columns` enum from the model macro and updated model methods to accept columns instead of strings[^27]
- Renamed persisted model writes to `.save()` and removed the generated persisted `.update()` method[^28]

LLM misses:
- The query matrix answer missed the intended semantics for try_ iteration and chunked results[^29][^30]
  Lesson: When describing iterator variants, explicitly verify error handling and yielded item shape for each variant before presenting a matrix
- The API answer assumed `Person::all()` and related methods needed to work without the prelude, but the requirement was to keep requiring it[^31]
  Lesson: Confirm crate ergonomics assumptions like import strategy before promising usage patterns

## ~/apps/Termsy

Worked on:
- Reviewed the README and clarified the app’s intended direction
- Evaluated SwiftNIO / NIOSSH for iOS compatibility and SSH stack choice
- Compared Ghostty wrapper/package options for embedding terminal rendering in Termsy
- Defined a short-term integration path using libghostty-spm and a minimal terminal spike

Accomplished:
- Chose NIOSSH as the SSH library direction for Termsy[^32]
- Selected Lakr233/libghostty-spm as the terminal integration package[^33][^34]
- Integrated libghostty-spm, added an echo terminal spike in ContentView, and got the build to succeed[^35]

LLM misses:
- Recommended theme-related Ghostty components even though the user said themes were unnecessary[^36][^37]
  Lesson: Respect explicit scope reductions and avoid proposing extra subsystems once the user has ruled them out

## ~/apps/pimux2000/pimux-server

Worked on:
- Adding structured server logging with tracing, including HTTP request logging and dependency updates
- Replacing raw println!/eprintln! usage in the server codebase while preserving CLI-facing output in command handlers
- Validating the logging changes with a clean build and test run

Accomplished:
- Structured logging was added to the pimux server using tracing, tracing-subscriber, and tower-http TraceLayer support[^38][^39]
- Raw println!/eprintln! calls were removed from the server modules, while CLI confirmation output in main.rs and list() was kept intact[^40][^41]
- The updated code built cleanly and all 72 tests passed[^42][^43]


[^1]: Implemented the real fix
[^2]: it shows up in the slash command menu in ios, but it seems to be just getting sent as a normal user message
[^3]: get rid of all of it and just put the startup stuff in a .task(priority: .low)
[^4]: I’m going to check whether this was added for a specific performance issue
[^5]: can we have our syncer just persist streamed messages and update contents instead of needing to track `@State …
[^6]: All references are gone
[^7]: All builds pass. Let me do a quick review of all changes
[^8]: All 72 tests pass. Let me verify the final state of each changed file
[^9]: ok so we can guarantee that messages from the server will _always_ have ids going forward
[^10]: i've updated the server to always return message IDs
[^11]: LLM: The pending message now uses the same layout as `UserMessageView`: baseline-aligned person icon + `VStack` with header + content
[^12]: Correction: can we animate messages coming in instead of the jump? or will that break layouts
[^13]: i updated everything, then tried to do `/todo` in the ios app. it didn't do anything at all
[^14]: Correction: no this overdoing it
[^15]: can we pass PimuxServerClient as an environment key instead of new-ing it up all the time
[^16]: The changes: - **`buildSelectLabels()`** — interleaves each todo with an indented `" delete"` option beneath i…
[^17]: request_planning_mode_exit` Behavior: - if planning mode is active, the agent can call that tool - …
[^18]: Changed: - `.pi/agent/extensions/planning-mode.ts` - added `Shift+Tab` as an extension shortcut to toggle plan…
[^19]: They were first added at the root in commit `73627ed` on `2026-04-02` - Later, commit `bbeb32b` moved 4 of the…
[^20]: Got it. Everything is clear now. Here's the final summary: --- **Goal:** Add "d" key to the `/todo` list picke…
[^21]: Keep planning. User direction: we can't do custom() ui Keep planning this. Stay in convo mode. Either ask the …
[^22]: Planning mode is still active — I can't edit files until it's turned off
[^23]: I can't turn off planning mode myself — the extension blocks my file edits and even my shell commands that look mutating
[^24]: ### What changed I moved the builder state bookkeeping into the main crate
[^25]: ### Split query into files Created: - `src/model/query.rs` — shared module root + common `build_query(...)` - …
[^26]: ### What changed I implemented both: 1
[^27]: I implemented it
[^28]: ### Changed - `Persisted` models now use **`.save()`** instead of `.update()` - removed the generated persiste…
[^29]: LLM: Here’s the clean matrix for the design you’re describing
[^30]: Correction: i just want to confirm: there should be no panics when using the try_ variant
[^31]: Correction: no it's ok to require the prelude
[^32]: Good call. For **Termsy**, I’d lock in: - **`swift-nio`** - **`swift-nio-transport-services`** - **`swift-nio-…
[^33]: let's just go with Lakr233/libghostty-spm for now
[^34]: **iOS-capable Ghostty integration** - **SwiftUI/UIKit wrapper layer** - **host-managed I/O**, …
[^35]: Now write the ContentView with an echo terminal spike
[^36]: LLM: **iOS-capable Ghostty integration** - **SwiftUI/UIKit wrapper layer** - **host-managed I/O**, …
[^37]: Correction: we don't need themes fwiw
[^38]: Got it. Here's the plan: ## Summary **Goal:** Add structured logging to the pimux server using `tracing`. **Re…
[^39]: All 72 tests pass. Here's a summary of everything that was done: ## Changes Made ### 1. `Cargo.toml` — added d…
[^40]: All `eprintln!`/`println!` removed from those files. Let me verify the agent/mod.rs still has the CLI-output ones only
[^41]: Those are all in the `list()` function which is CLI output — correct to leave as-is
[^42]: Clean build — the 3 warnings are pre-existing (not from our changes). Let me verify all the changes look correct
[^43]: All 72 tests pass. Here's a summary of everything that was done: ## Changes Made ### 1. `Cargo.toml` — added d…


---

# Report for 2026-04-03

# Daily report for 2026-04-03

## ~/apps/Termsy

Worked on:
- SSH/terminal input handling and key event routing
- Ghostty keyboard protocol behavior and terminal rendering quirks
- Autoconnect tab startup behavior and tab bar UX
- Terminal tab layout, dragging, and selection interactions

Accomplished:
- Applied a quick viewport type fix using `UInt16`[^1]
- Added logging to diagnose a silent SSH connection hang[^2]
- Implemented changes so hardware keypresses route through `ghostty_surface_key` and `insertText` is suppressed for them[^3]
- Verified the build succeeds and cleaned up stale Xcode project references after deleting a file[^4][^5]

LLM misses:
- Assumed the SSH issue was only a logging problem, but the user clarified the connection could work from other terminals and later that the app still had no terminal output[^6][^7][^8][^9]
  Lesson: Confirm whether a failure is app-specific versus environment-specific before narrowing the diagnosis
- Suggested a tab startup implementation when the user was actually asking for a more native, richer tab interaction model[^10][^11][^12]
  Lesson: Distinguish between a minimal functional answer and a request for platform-native behavior and interaction details
- Iterated on tab styling and reordering, but the user still reported poor click targets, layout shifts, and an overall bad feel[^13][^14]
  Lesson: When UX feedback is negative, validate the full interaction feel—not just the visual layout or basic reordering logic

## ~/apps/seekwel

Worked on:
- Refactoring the SQL layer toward a more modular, maintainable structure
- Clarifying the boundary between internal SQL generation and the public crate API
- Reviewing user-input handling and SQL injection risk in the query layer

Accomplished:
- Completed a first-pass SQL refactor with no behavior change, splitting `src/sql/mod.rs` into focused modules
- Made the SQL layer internal by changing `pub mod sql;` to `mod sql;`, and added a real internal statement model for `SELECT` queries[^15]


## ~/apps/pirot

Worked on:
- Planning-mode keybinding behavior in the pirot project
- Authentication flow fallback when standard auth fails

LLM misses:
- Misread the user’s intent about Shift-Tab, treating planning mode as already correctly mapped instead of responding to the correction[^16][^17]
  Lesson: When a user corrects a keybinding or behavior claim, verify the exact intended mapping before asserting the current setup

## ~/apps/pimux2000

Worked on:
- Wired the at-completions feature through the server protocol and request handling
- Added server-side plumbing for fulfillment and cleanup across the live store / IPC stack
- Implemented the iOS client-side @ completion support in the composer and session view integration

Accomplished:
- Completed the at-completions feature wiring across server and iOS; both builds and tests passed[^18]
- Server implementation covered the protocol, HTTP endpoint, agent handling, and cleanup paths for at-completions


[^1]: Viewport uses `UInt16`. Quick fix
[^2]: No SSH logs at all — the connection is hanging silently. Let me add logging to find where it stalls
[^3]: Now every hardware keypress goes through `ghostty_surface_key` only, and `insertText` is suppressed for all of them
[^4]: Build succeeds. Let me also verify the `TerminalSessionView` file reference is removed from the Xcode project …
[^5]: Clean — no stale references. Let me verify the final state of all changed files
[^6]: LLM: Try pressing ctrl+a and check the console logs for `[KEY]` output
[^7]: Correction: no output when i press ctrl+a
[^8]: LLM: The crash in `startShell` is preventing everything — the view never gets created
[^9]: Correction: no, even when it doesn't crash, there's no term
[^10]: LLM: On launch, `ContentView` queries all sessions with `autoconnect == true`, ordered by most recently connected, and opens a tab for each one
[^11]: Correction: it should support: dragging to reorder, hover preview, cmd-# to jump by index, etc
[^12]: when the app starts, we should automatically open tabs for all of the autoconnect sessions
[^13]: LLM: Stripped way back. Here's what's different: - **No `GeometryReader`** — tabs use `.frame(maxWidth: .infinity)`…
[^14]: Correction: still terrible feeling. you have to click the actual text instead of the whole tab, the layout STIL shifts, it's just bad all around
[^15]: changed `pub mod sql;` → `mod sql;` So the SQL layer is no longer part of the public crate API
[^16]: i want shift-tab to toggle planning mode instead of thinking
[^17]: It already is. Relevant files: - `.pi/agent/extensions/planning-mode.ts` — registers `shift+tab` to toggle pla…
[^18]: Both server and iOS build, tests pass


---

# Report for 2026-04-04

# Daily report for 2026-04-04

## ~/apps/Termsy

Worked on:
- Terminal tab behavior and multi-connection support
- Live UI tuning for the tab bar, glass pills, and centered labels
- Settings-driven configuration for visual and input behavior
- Scrolling calibration and trackpad/mouse sensitivity
- App lifecycle and shell/connection behavior on focus changes

Accomplished:
- Updated tab handling to use UUIDs and support more than one connection to the same host[^1][^2]
- Kept all tab controllers mounted while switching the active one, to preserve tab state during navigation[^3][^4]
- Adjusted scroll sensitivity ranges to make scrolling feel faster and more usable[^5][^6]
- Removed the hover recognizer after it caused issues, and verified the rollback with a build check[^7]
- Changed the mouse path more fundamentally to address focus-change errors[^8]

LLM misses:
- A visual fix was proposed, but the user reported that neither the corner-radius nor glass styling changes were visible[^9][^10]
  Lesson: When UI changes are hard to verify, prioritize making the effect observable in the simulator before iterating on styling details
- The UI was steered toward a Sheet-based presentation, but the user preferred using an Inspector instead[^11][^12]
  Lesson: Confirm the intended presentation surface early when a UI workflow can live in multiple container types
- Scrolling behavior was treated as a hardcoded fix, but the user wanted the values exposed as settings[^13][^14]
  Lesson: Prefer configuration-first solutions when the user asks to avoid hardcoding behavior

## ~/apps/pimux2000

Worked on:
- Message rendering and layout behavior for tool output and markdown content
- Scrollable conversation UI polish, including bottom-jump behavior and message spacing
- Empty/loading states and slash-command handling
- Sidebar grouping and disclosure header design by cwd and host metadata

Accomplished:
- Adjusted collapsed message links to use role-specific labels for tool results, bash output, and other content[^15]
- Fixed markdown sizing so constrained content respects hard height caps and doesn’t expand layouts unexpectedly[^16]
- Added a better empty state for transcripts when no server is configured[^17]
- Implemented sidebar grouping by host location and cwd, then refined the disclosure headers so they primarily show cwd with host captions only for duplicates[^18][^19]
- Tightened slash-command validation so slash-prefixed input is no longer treated as a normal user message[^20]


## ~/apps/seekwel

Worked on:
- Reviewed next-step direction for the repo, including whether to keep focusing on core CRUD/ordering or move into richer data-model features
- Restructured the README to make the model examples easier to skim without adding extra ceremony
- Defined and implemented first-class forward relations with a minimal `BelongsTo<T>` design
- Refined comparison ergonomics to avoid `None::<u64>` at call sites by planning null-specific comparison operators

Accomplished:
- README was reorganized into smaller sections instead of one giant models block, while keeping the tone and content intact[^21]
- First-pass `BelongsTo` relations were implemented with typed foreign-key storage, lazy parent loading, per-instance caching, and reload-based cache clearing[^22]
- The next relation milestone was concretized as first-class forward relations with a small, shippable `BelongsTo<T>` design[^23]

LLM misses:
- Initially pushed back on relations/associations as too expensive a next step, but the user clarified they should be the next major direction
  Lesson: When the user frames a strategic direction, align quickly and avoid over-defending a prior roadmap judgment

## ~/apps/pirot

Worked on:
- /convo summary handling for open questions in the convo extension
- /bump flow when no manifest is present, including commit/push behavior

Accomplished:
- Fixed open-questions summary parsing so closing remarks after “None” no longer cause unresolved-question false positives[^24]
- Updated /bump to continue without a manifest by skipping version-bump steps and proceeding to stage, commit, and push[^25]


## ~/apps/hyperlinks

Worked on:
- RSS feed import support for Hyperlinks, including backfill vs forward-only import
- Per-feed controls for manual sync and pausing sync while keeping feed records
- Schema and controller updates for RSS feed storage and linking imported URLs to their source feed
- Home page link card presentation and responsive grid layout

Accomplished:
- Added schema support for linking imported hyperlinks to their RSS feed, including a migration path for existing databases[^26][^27]
- Implemented the feeds controller and registered the new entity in schema sync[^28]
- Spawned the feed poller in server startup and resolved the processing queue wiring needed for it[^29][^30]
- Verified the RSS import work with tests and build checks after fixing a trailing-slash normalization issue[^31][^32]
- Updated the home page to use a responsive grid for links and changed cards to show the host instead of the full URL[^33][^34]


[^1]: we should be able to open more than one connection (tab) to the same host
[^2]: Now I'll give `TerminalTab` its own UUID and update all the wiring
[^3]: Now I’m keeping all tab controllers mounted and toggling which one is active/visible
[^4]: Now I’m changing the container to keep all tabs alive and just hide the inactive ones
[^5]: now scrolling is painfully slow. i don't think the settings values are wide enough
[^6]: I’m widening both ranges now, with a much larger trackpad/mouse range since precise scrolling changed the cali…
[^7]: I’ve removed the hover recognizer. Now I’m building to confirm the rollback is clean
[^8]: I changed the mouse path in a more fundamental way. ### What I changed
[^9]: LLM: Clean build. Three fixes: **Rounded corners** — Added `layer.cornerCurve = .continuous` and `layer.masksToBoun…
[^10]: Correction: neither of the visual changes worked. how can we make it so you can actually see what you're doing instead of making me relaunch the simulator every time
[^11]: LLM: No dark rectangle artifact — both pills are clean with fully transparent backgrounds
[^12]: Correction: also maybe we put it in an Inspector instead of a Sheet
[^13]: LLM: Fixed in `Termsy/Views/TerminalView.swift
[^14]: Correction: can we add a setting for those things instead of hardcoding them
[^15]: Done
[^16]: Good. Here's a summary of all changes: **`MarkdownTextView.swift`** — 3 fixes: 1. `sizeThatFits` returns `.zer…
[^17]: Build succeeds. Here's a summary of the three changes: 1. **`SessionTranscriptView.swift`** — Added `.noServer…
[^18]: groups sidebar sections by `(host.location, cwd)` - section headers now show `host • ~/path` - same cwd on dif…
[^19]: each `(host.location, cwd)` group is now a `DisclosureGroup` - groups start expanded by default - collapsing i…
[^20]: Agreed. I tightened this so slash-prefixed input is no longer allowed to degrade into a normal user message. #…
[^21]: Done: `README.md` What changed: - split the big `models` block into smaller sections - kept the same overall c…
[^22]: Implemented first-pass `BelongsTo` relations
[^23]: Resolved summary with all remaining design choices fixed: ### Goal Add **first-class forward relations** to `s…
[^24]: **What was happening:** `getSummarySectionLines("open questions")` collects all lines after the "Open questions" heading until it hits another recognized headin…
[^25]: Looking at the code, when no manifest is found, `chooseManifest` throws
[^26]: Added `ALTER TABLE hyperlink ADD COLUMN rss_feed_id ...` that runs during schema sync - Wrapped it in `add_col…
[^27]: The `CREATE TABLE IF NOT EXISTS` won't add new columns to an existing table
[^28]: Now register the new entity in schema sync
[^29]: Now spawn the feed poller in the server start function
[^30]: Good, `ProcessingQueueSender` is just `ProcessingQueue` which derives `Clone
[^31]: Just one test failure — `feed-rs` normalizes the URL with a trailing slash. Let me fix the test
[^32]: All 330 tests pass. Let me do a final build to confirm warnings
[^33]: I made it responsive
[^34]: card now shows the link host instead of the full URL - full URL is still used for the anchor `href` - I also l…


---

# Report for 2026-04-05

# Daily report for 2026-04-05

## ~/apps/seekwel

Worked on:
- Comparisons and null handling in the query layer
- HasMany association design and macro/runtime support
- Planning clearer association naming and file organization
- Designing first-class FTS support
- Designing safer schema reconciliation from live DB state

Accomplished:
- Implemented `Comparison::IsNull` and `Comparison::IsNotNull` support[^1]
- Added runtime `HasMany` relation support and exported it through the model/public API[^2]
- Moved `HasMany` to a `u8` const key and had the macro generate named column-key consts for cleaner usage[^3]
- Resolved the FTS planning gaps with explicit assumptions and updated the plan[^4]
- Resolved the schema-change planning gaps with explicit assumptions and updated the plan[^5]

LLM misses:
- The HasMany design initially pushed toward a macro/type-rewriting approach when the user preferred a const-generic, nightly-friendly direction[^6]
  Lesson: When the user asks for a less invasive type-level design, validate whether a const-generic or type-state approach can satisfy the API before proposing macro rewriting
- The HasMany key was first modeled as `usize`, but the user pushed for a smaller representation[^7][^8]
  Lesson: Prefer the smallest practical internal representation when it preserves the API; re-check storage width assumptions before settling on a type

## ~/apps/Termsy

Worked on:
- Terminal input and mouse interaction behavior on iPad
- Scrolling behavior in Termsy, including smooth/inertial motion
- Session replay / tmux display artifacts
- App settings, keyboard shortcuts, and terminal state handling
- Generating an auto-created changelog/summary view from the app build

Accomplished:
- Removed the SSH-dropped warning UI from the terminal controller[^9]
- Rewrote the whats-new generation script to be pure shell by removing Python and text filtering steps[^10]

LLM misses:
- Mistook a Termsy-specific key-repeat problem for a broader system-wide iPad keyboard setting issue[^11]
  Lesson: When the user scopes a bug to the app, avoid platform-wide troubleshooting unless the app-specific path has been ruled out
- Focused on the wrong touch interaction model instead of the requested two-finger drag-to-scroll behavior[^12][^13]
  Lesson: Treat gesture requests literally and validate the exact interaction mapping before proposing implementation details
- Pursued a smooth-scrolling approach that did not match the actual tmux line-step behavior and failed to deliver the requested in-between animation[^14][^15]
  Lesson: Verify the terminal’s real scroll granularity and rendering path before designing animation around assumptions

## ~/apps/pirot

Worked on:
- Reducing convo mode eagerness and stickiness in `.pi/agent/extensions/convo.ts
- Tuning clarification detection so it asks more concrete, lower-level questions
- Adjusting the retry/follow-up flow after `agent_end` to avoid staying in convo mode too long

Accomplished:
- Identified the convo extension as the main source of over-eager entry and sticky behavior[^16][^17]
- Confirmed that the retry loop in `agent_end` was re-steering into clarification mode[^18][^19]
- Pinned down that a bare `?` should not trigger or prolong convo mode[^20][^21]

LLM misses:
- Initially treated the issue as open-ended clarification work instead of recognizing the pasted lines as the bug itself[^22]
  Lesson: When the user says a quoted line is the bug, respond by resolving the implementation issue directly rather than asking for more information

## ~/apps/pimux2000

Worked on:
- iOS app review and fixes
- Server-side review in pimux-server
- Concurrency warning cleanup
- Screenshot generation and transcript/layout issues

Accomplished:
- Applied fixes to session read-state and related app issues, then verified tests/build were passing[^23]
- Implemented concurrency-warning cleanup, including making pure helpers and data types nonisolated where appropriate[^24][^25]
- Updated compaction behavior so headless compacting re-discovers and re-summarizes the session after compaction[^26]

LLM misses:
- Screenshot work initially assumed iPad captures without the required landscape orientation[^27][^28]
  Lesson: Confirm platform-specific screenshot orientation requirements before implementing or summarizing the work

## ~/apps/hyperlinks

Worked on:
- Home page card layout and typography adjustments
- Badge placement for imported links on compact cards
- Meta line formatting for RSS and PDF badges

Accomplished:
- Reduced the home page grid to 2 columns on large screens and tightened card title wrapping to improve alignment[^29]
- Shrank the home page card typography for a more compact layout[^30]
- Added an RSS badge for feed-imported links and moved it to the meta line before the timestamp without a separator[^31][^32]
- Moved the PDF badge to the meta line on compact/home-page cards, before the timestamp, while preserving its non-compact placement[^33]


[^1]: Reworked `Comparison` from a generic enum into a concrete comparison type with: - constructors: - `Comparison:…
[^2]: ### What changed - Added `HasMany` runtime relation support in: - `src/model/relation.rs` - Exported it from: …
[^3]: ### Changed `HasMany` now uses a `u8` const key, and the macro generates named column-key consts on the `<Mode…
[^4]: Resolved the remaining gaps with explicit assumptions
[^5]: You’re right — here is the **fully closed** version with the remaining policy choices made explicit
[^6]: Correction: can't we use a const generic for HasMany<Pet, PetColumns::OwnerId> instead of rewriting with the macro
[^7]: LLM: I did some digging, and the short answer is: ### The exact nice form we want is probably **not achievable toda…
[^8]: Correction: could we go u8 instead of usize? usize seems unnecessarily large
[^9]: Agreed. I changed it so that message is never shown. What I changed: - `Termsy/Views/TerminalTabBarController.…
[^10]: ### Changed - Rewrote `ci_scripts/generate_whats_new.sh` to be **pure shell** - Removed: - Python - filtering …
[^11]: Correction: i mean only in termsy
[^12]: LLM: Hardware `UIKeyInput` callbacks are now suppressed for the whole time a physical key is held, not just the first press
[^13]: Correction: can we make two finger drags on the screen scroll instead of select text
[^14]: LLM: You were right — the first pass had a real flaw
[^15]: Correction: no downward momentum but now the whole window scrolls weirdly which is even worse
[^16]: yeah, I think your read is basically correct
[^17]: the line you pasted is basically the smoking gun
[^18]: “You still need more information...” - “Do not stop with prose.…
[^19]: agent_end` decides clarification is still needed - `questionnaireRetryCount` increments - then i…
[^20]: looksLikeClarificationRequest(...)` currently treats any `?` as a clarification…
[^21]: A bare `?` is not a real missing requirement here, and it should not reopen or prolong convo mode
[^22]: No — that pasted line is the bug itself
[^23]: All tests pass. Let me verify there were no failures
[^24]: can we fix all the concurrency warnings
[^25]: Sure. Let me walk through each warning category: --- ### Category 1: Codable types used inside a non-MainActor…
[^26]: can we re-summarize sessions on compaction
[^27]: LLM: ### Added - `pimux2000/Models/ScreenshotScenario.swift` - `pimux2000UITests/ScreenshotTests.swift` - `scripts/…
[^28]: Correction: the ipad screenshots should be landscape
[^29]: Yeah, agreed — the 3-column version was too cramped
[^30]: marks home-page cards as “compact” - `templates/hyperlinks/show.stpl` - leaves discovered-link cards at n…
[^31]: I added an `RSS` badge for feed-imported links on the home page cards
[^32]: removed `RSS` from the host row - now renders it in the meta row as: - time - status if present - `RSS` - `Det…
[^33]: on non-compact cards, `PDF` stays where it was Verified: - `cargo check` ✅ - `cargo test index_renders_p…


---

# Report for 2026-04-06

# Daily report for 2026-04-06

## ~/apps/Termsy

Worked on:
- Inspector and overlay behavior
- Terminal resize / scroll stability
- Font source filtering and platform compatibility
- Session picker navigation and native key affordances

Accomplished:
- Reworked the inspector into a custom trailing overlay panel instead of resizing the terminal[^1]
- Removed the dark dimming overlay from the custom panel[^2]
- Fixed tmux scroll-jitter by suppressing unnecessary SSH window-change forwarding[^3]
- Added keyboard navigation to the new-tab session picker, including arrow keys, Return, and Esc[^4]
- Addressed macOS SwiftUI availability issues with a shared compatibility helper[^5]

LLM misses:
- Assumed session identity should use a UUID-like key, but the user wanted a natural composite key[^6][^7]
  Lesson: When the user proposes a simpler domain key, prefer it over inventing a surrogate identifier unless there is a clear technical need
- Introduced a dark overlay while implementing the inspector panel, but the user wanted the panel without dimming[^8][^9]
  Lesson: Match UI changes to the exact visual requirement; avoid adding default modal-style chrome unless requested

## ~/apps/pimux2000

Worked on:
- Refactoring and simplifying the iOS session view
- Renaming the server binary from `pimux-server` to `pimux
- iPad screenshot generation and dark-mode screenshot behavior
- iOS app polish and sidebar interaction fixes
- Adding a `pimux server status` command

Accomplished:
- Reduced `PiSessionView.swift` substantially by extracting dialog logic into focused files[^10][^11]
- Completed the binary rename migration to `pimux` across build and release configuration[^12]
- Fixed the iPad screenshot capture flow so screenshots are no longer portrait/cropped in the simulator path[^13]
- Completed an iOS sidebar fix and verified the test suite passed cleanly[^14][^15]

LLM misses:
- Assumed screenshot example-data changes should be made in simulator fixtures, but the user meant data from the local computer[^16][^17][^18]
  Lesson: Confirm the target environment before proposing fixture or simulator-based changes

## ~/apps/pirot

Worked on:
- Refining `/convo` behavior in the Pi extension
- Clarifying how extension-authored guidance is represented in chat
- Considering LSP support and why it may not be taking effect

Accomplished:
- Removed the automatic jump into convo mode and replaced it with a UI hint to use `/convo` manually[^19]
- Switched `/convo` steering messages from user-visible messages to custom internal directives, making them distinguishable from real user input[^20]


## ~/apps/talk

Worked on:
- Homepage content expansion for new language examples
- Implementing and stabilizing `let else
- Fixing scope/capture behavior in name resolution
- Making the `let else` example runnable in the site content

Accomplished:
- Added multiple examples to `www/content/index.md`, including closures, trailing block callbacks, custom initializers, `if let`, `let else`, structural matching, imports/exports, and a tiny HTTP server[^21]
- Fixed the underlying `let else`-related crash by adjusting scope capture handling in `src/name_resolution/name_resolver.rs`[^22]
- Restored the `let else` block in `www/content/index.md` to runnable form and verified the site rebuild and example execution[^23]


## ~/apps/sshtestin

Worked on:
- Defining how the Rust crate should run on a host and handle SSH connections with a TUI
- Choosing an SSH server port for development and deployment

Accomplished:
- Confirmed that port 22 is acceptable for the SSH server, with 2222 only suggested as a safer development default[^24]


## ~/apps/wink

Worked on:
- LED matrix layout and wiring on the XIAO ESP32C6
- 2D lighting effects for a 10x6 / 6x10 RGB grid
- Calibration and effect tuning for the panel mapping
- Locking the display to a scanner animation

Accomplished:
- Added a 2D grid effect engine with multiple lighting effects for the 60-LED panel[^25]
- Corrected the likely panel mapping to 6x10 with serpentine columns and a top-left origin[^26]
- Simplified calibration mode to make layout evaluation easier[^27]
- Locked the active display to the scanner effect and removed effect cycling[^28]

LLM misses:
- The calibration mapping was simplified incorrectly by assuming a snake-back layout when the user clarified it should continue in the same direction[^29][^30][^31]
  Lesson: When the user corrects a layout detail, re-evaluate the mapping immediately instead of reinforcing the prior assumption

## ~/apps/pimux2000/pimux-server

Worked on:
- Added optional Postgres backup support for the server’s incoming agent messages
- Adjusted the database design to use a messages table plus a sessions table
- Added a CLI backfill mode for loading historical transcripts
- Updated the transcript-fetch path to accept a host parameter for backfill requests

Accomplished:
- Implemented optional Postgres backup writing with automatic schema creation and reconnect handling[^32]
- Reworked the storage model to use separate sessions and messages tables, removing the earlier single backup table approach[^33]
- Added a host parameter to transcript retrieval and wired the backfill command to use it[^34]


## ~/apps/seekwel

Worked on:
- Designing safer schema changes for seekwel by comparing live SQLite schema against model definitions and reconciling differences
- Refactoring seekwel-macros to separate macro entrypoints, model wiring, analysis, and code generation
- Planning FTS indexing support using `FTS<...>` field syntax in `#[seekwel::model]`
- Discussing the model-definition surface, including removing user-facing `#[derive(Model)]` in favor of `#[seekwel::model]`
- Brainstorming transaction support and related callback/hook design for the library

Accomplished:
- Completed a macro refactor that split `seekwel-macros` into phased modules for entrypoints, wiring, analysis, and code generation[^35]
- Resolved the schema-safety plan by explicitly settling the remaining ambiguity around how v1 identifies seekwel-managed live tables[^36][^37]
- Closed the FTS design plan enough to proceed with a documented approach for model field syntax and indexing behavior[^38]

LLM misses:
- Kept treating the work as an implementation task when the user wanted continued planning and a design document, not code changes[^39][^40]
  Lesson: When the user says to stay in convo mode and keep planning, stay strictly in design/discovery framing and avoid drifting into implementation-oriented language
- Initially suggested asking more questions instead of directly using the requested questionnaire workflow[^41]
  Lesson: If the user explicitly requests convo_questionnaire, follow that mechanism immediately rather than paraphrasing the need for more clarification

[^1]: Yep. I stopped fighting the resize behavior and removed the real inspector. What I changed: - `ContentView.swi…
[^2]: Done — removed the dark overlay
[^3]: I made a surgical fix for this
[^4]: I added keyboard navigation to the new-tab session picker
[^5]: fair. This was turning into whack-a-mole, so I fixed the **whole class** of SwiftUI platform-availability issu…
[^6]: LLM: Yep. For Termsy, the clean version is: - **keep GRDB as the local store** - **sync only session profile record…
[^7]: Correction: why not just use username/hostname/tmuxsession as the key instead of uuid
[^8]: LLM: Yep. I stopped fighting the resize behavior and removed the real inspector. What I changed: - `ContentView.swi…
[^9]: Correction: we don't need the dark overlay
[^10]: No explicit file references — the project uses automatic file scanning
[^11]: **PiSessionView.swift: 1,669 → 1,086 lines** (35% reduction)
[^12]: ### What changed - `pimux-server/Cargo.toml` - added an explicit bin target named `pimux` - set `default-run =…
[^13]: You're right — the previous version was still effectively using the **portrait XCTest attachment path** for iPad
[^14]: All tests pass, no failures, no warnings
[^15]: 54 tests passed, zero failures, zero warnings. Let me check the final line counts
[^16]: LLM: Yes. The clean way is to add it in the deterministic screenshot fixtures: - `pimux2000/Models/UITestFixtures.s…
[^17]: Correction: i mean on this computer. not in the simulator
[^18]: can we add more example data to the screenshots
[^19]: **No more automatic jump into convo mode** - removed the `agent_end` path that auto-switched into convo - now …
[^20]: Added a custom message type: - `convo-directive` - Added …
[^21]: closures + captured state - trailing block callbacks - custom initializers - `if let` - `let else` - structura…
[^22]: added `capture_boundary` to `Scope` - capture lookup now only records captures across real capture boundaries …
[^23]: site rebuilds ✅ - the `let else` example runs ✅ Verified with: ```talk enum Maybe<T> { case some(T) case none …
[^24]: You can use **22**. I only said **2222** because it’s the safer **development** choice. ### Why I suggested 22…
[^25]: Added a grid animator with 6 effects: - diagonal rainbow - plasma - twinkle - ripple - scanner - aurora - Uses…
[^26]: grid size: `6 x 10` - mapping: **serpentine columns** instead of serpentine rows - origin default: **top-left*…
[^27]: no more cycling through layout guesses - no bars - no multiple colors/patterns at once - lower brightness What…
[^28]: fixed active effect to `EffectKind::Scanner` - removed effect cycling logic Verified: - `cargo check` passes I…
[^29]: LLM: no more cycling through layout guesses - no bars - no multiple colors/patterns at once - lower brightness What…
[^30]: Correction: no wait, 3 is continue same direction
[^31]: 1. top left, 2. across a row 3. snake back the other way
[^32]: new optional Postgres backup writer - auto-creates `agent_message_backups` - retries on reconnect if Postgres …
[^33]: changed it. Now: - no raw `agent_message_backups` table - Postgres schema is: - `sessions` - `messages` What i…
[^34]: pimux server backfill` now uses that param when fetching transcripts - host-scoped requests bypass a cached tr…
[^35]: ### Macro refactor Split `seekwel-macros` into phases: - `seekwel-macros/src/lib.rs` - now just the proc-macro…
[^36]: Understood. I’m resolving the remaining implicit gap explicitly: **how v1 knows which live tables are actually…
[^37]: Resolved with the latest answers plus one explicit safety assumption for the remaining type-comparison gap
[^38]: ## Goal Produce a **design document** for adding **FTS indexing** to `seekwel`, using model field syntax like:…
[^39]: Keep planning. User direction: we should kill user-facing #[derive(Model)] during this Keep planning this. Sta…
[^40]: Keep planning. User direction: the final endpoint of this plan is a document. not writing code. You are still …
[^41]: Asked the next two highest-value clarification questions: 1


---

# Report for 2026-04-07

# Daily report for 2026-04-07

## ~/apps/pimux2000

Worked on:
- iOS session view behavior and image rendering
- Transcript scrolling performance and stability
- Sidebar session selection flicker/stability
- Determining active TUI session status
- Project-based daily reporting and report formatting

Accomplished:
- Added tests for UUID detection and sync no-op behavior; verified the full test suite passed[^1][^2]
- Implemented transcript rendering and scrolling fixes, replacing non-selectable transcript text with labels and validating a clean build with all tests passing[^3][^4]
- Delivered a summary of the report format change to include project-based excerpts and remove host/session-oriented output[^5]

LLM misses:
- Initially framed the daily report around sessions and hosts instead of a project-based view[^6][^7][^8][^9]
  Lesson: Anchor the report structure on the project requirement first; avoid introducing session/host aggregation unless explicitly requested
- Treated excerpts as report-level extras rather than attaching them per accomplishment[^10][^11]
  Lesson: When a user asks for excerpts, bind them to each accomplishment so the narrative stays evidence-backed
- Shifted the discussion toward report-generation mistakes instead of the user's requested focus on mistakes made across sessions/messages[^12][^13]
  Lesson: Distinguish between flaws in the reporting pipeline and flaws in the underlying assistant behavior being reported on

## ~/apps/Termsy

Worked on:
- macOS app shell and tab behavior
- macOS UI simplification and native tabs
- macOS appearance, transparency, and blur handling
- macOS app icons and screenshot sizing
- SSH session persistence and connection reliability

Accomplished:
- Added Ghostty-like macOS background controls for opacity and blur, including an off/glass mode[^14]
- Adjusted macOS titlebar/window transparency so it uses the same window transparency instead of getting double-tinted[^15]
- Updated macOS app icon assets and screenshot sizing work
- Fixed saved sessions so entries with the same host but different tmux names persist separately[^16]
- Updated font settings in macOS Settings with font selection and installed/custom fonts support[^17]

LLM misses:
- Assumed the macOS startup flow was mainly about launching the local shell, but the user wanted the default new-tab page replaced with a home-directory shell[^18]
  Lesson: When the user corrects scope, re-evaluate the UX goal instead of refining the implementation detail
- Kept proposing a custom macOS tab/sidebar/header layout when the request was to remove those UI elements and use native tabs[^19][^20]
  Lesson: Treat platform-native UI guidance as a strong constraint, not a design preference
- Focused on a toolbar-related change even after the user clarified that macOS should have no toolbar at all[^21][^22]
  Lesson: If the user says an entire UI surface is unnecessary, remove it rather than iterating on it
- Treated the icon issue as a straightforward size fix, but the real request was to match Ghostty-style appearance across pre-Tahoe and Tahoe macOS[^23][^24]
  Lesson: When asked about visual parity, consider platform-specific styling conventions, not just asset dimensions
- Interpreted the settings-page request as a general macOS settings addition, but the user specifically wanted Ghostty-like behavior and a resizable settings page[^25]
  Lesson: Match the requested product reference and interaction requirement together; don’t satisfy only the feature category

## ~

Worked on:
- Migrating the backup Postgres setup to an Ubuntu-based instance and updating dependent services
- Updating the pimux server to point at the new Postgres configuration
- Adjusting backfill behavior so deployment files capture the Postgres connection used

Accomplished:
- Updated the PostgreSQL migration path and confirmed the new database was reachable for dependent services[^26][^27]
- Created the pimux role and database on the new Postgres and updated the server to use it[^28]
- Simplified the environment forwarding approach after review and kept the implementation passing tests[^29][^30]
- Added backfill logic to update the service install file with the Postgres connection it discovers[^31]

LLM misses:
- The assistant initially assumed the existing cluster/host context instead of following the user’s correction about the Ubuntu-based Postgres migration plan[^32][^33]
  Lesson: When the user corrects infrastructure direction, reset assumptions and align immediately with the revised target state

## ~/apps/talk

Worked on:
- Browser-safe examples and docs wording
- Audit review of the codebase for clarity, correctness, and performance
- Writing detailed audit docs under `docs/
- Refining the A09 hotspot-file assessment toward architecture and lowerer/inference pain points
- Planning a `ProgramLowerer` vs `FunctionLowerer` split

Accomplished:
- Removed the misleading claim that something works on the CLI but not in the browser from `www/content/index.md`[^34]
- Created a docs index plus one detailed markdown file per audit issue, including the A09 hotspot doc[^35]
- Updated the A09 doc to center architectural entanglement and the lowerer/inference files as the real pain points, not file shuffling alone[^36]

LLM misses:
- Initially framed non-browser-runnable examples as acceptable, but the user clarified they should not be shown at all[^37][^38]
  Lesson: When the runtime target is strict, avoid leaving in examples that cannot execute in that target, even as illustrative snippets

## ~/apps/pirot

Worked on:
- Verified rust-analyzer workspace diagnostic capability handling in the pirot project
- Checked whether the client was reading rust-analyzer’s initialize response correctly
- Confirmed behavior inside a real Rust workspace to rule out cwd-specific probing issues

Accomplished:
- Confirmed that local rust-analyzer advertises `workspaceDiagnostics: false` even in a real Rust workspace[^39]
- Verified the extension is reading rust-analyzer’s diagnostic capability response correctly[^40]


## ~/apps/pimux2000/pimux-server

Worked on:
- Backfill error handling and reporting behavior
- Backfill progress and periodic logging
- Host identity canonicalization and normalization

Accomplished:
- Backfill became best-effort: failures are reported but do not stop the overall run[^41]
- Added periodic backfill logging, including start notices and progress updates every 25 processed items[^42]
- Completed cleanup and normalization of the local and remote host identity registry[^43]

LLM misses:
- Initially suggested using the .local hostname form as canonical instead of removing it from canonical locations[^44]
  Lesson: When a user specifies a naming convention change, align canonicalization with that target rather than proposing a workaround that preserves the old form

[^1]: All tests pass. Now let me also add a test for the UUID detection and verify the sync no-op behavior. Let me f…
[^2]: All existing calls already pass `summary
[^3]: Good. I have a full picture now. Let me implement all four fixes. ### Fix A: Replace `UITextView` with `UILabe…
[^4]: Clean build, zero warnings. Now run all tests
[^5]: Yes. Updated v1: - report is still **project-based** - **no host list** - **no session counts** - include an *…
[^6]: LLM: that Postgres archive is exactly the right source for this
[^7]: Correction: it should be project-based, not session based
[^8]: LLM: I’d change the design to **project aggregation**, not session aggregation
[^9]: Correction: we don't need sessions or hosts in the report. but can we include some excerpts
[^10]: LLM: Yes. Updated v1: - report is still **project-based** - **no host list** - **no session counts** - include an *…
[^11]: Correction: the excerpts should be per accomplishment
[^12]: LLM: Ah — yes, that’s a different and much better thing
[^13]: Correction: i'm not talking about mistakes in report generation
[^14]: What changed - Added Ghostty-like macOS background controls: - `Background > Opacity` - `Background > Blur` - …
[^15]: Fixed both points: - I removed the extra titlebar/frame tint layer, so the titlebar
[^16]: saved sessions with the same host but different tmux names are just overwriting
[^17]: macOS Settings now has a **Font** section with: - **Current Font** - **System Monospaced Fonts** - **Installed…
[^18]: Correction: we should also just open a local shell in the home directory instead of showing the new tab page on macos
[^19]: LLM: ### Changed - **`TermsyMac/TermsyMacApp.swift`** - macOS now auto-opens a local shell when the window first ap…
[^20]: Correction: we don't need the sidebar or header on mac. and we should be using native tabs
[^21]: LLM: ### What changed - **Removed the macOS sidebar/split view UI** - **Removed the in-content custom tab strip/hea…
[^22]: Correction: we don't need a toolbar at all
[^23]: LLM: I fixed the macOS asset sizes that were in the repo
[^24]: Correction: no that was good. but how can we make the icons look at home on pre-tahoe macos as well as tahoe macos
[^25]: Correction: whatever the Ghostty app does. and the settings page should be resizable
[^26]: i set up the new postgres container. you can ssh into it at root@pg. the old container is at root@postgres
[^27]: Good — remote md5 auth is enabled and PG is listening on all interfaces. Now update teslamate
[^28]: All set. Here's what I did: - **Created** `pimux` role and database on the
[^29]: All 94 tests pass. Now when `pimux server install` runs, it'll pick up `PIMUX_BACKUP_POSTGRES_URL` from the cu…
[^30]: All passing. Also don't need the `pub(crate)` on the module anymore
[^31]: All passing. The change is small — right after `backfill` successfully connects to postgres, it calls
[^32]: LLM: Your cluster has **5 nodes**, all online: | Node | CPUs | RAM | Status | |------|------|-----|--------| | **ar…
[^33]: Correction: i want to spin up a new postgres instance on ark based on ubuntu instead of alpine, then migrate the alpine po…
[^34]: Agreed. I removed the “works from the CLI but not in the browser” line from `www/content/index.md`. I left the…
[^35]: Done
[^36]: Updated `docs/audit/A09-large-hotspot-files.md`. The doc now focuses on
[^37]: Agreed. I removed the “works from the CLI but not in the browser” line from `www/content/index.md`. I left the…
[^38]: i meant that we shouldn't even show examples that can't be run in the browser
[^39]: I re-ran the probe inside a real Rust workspace (`/Users/nakajima/apps/guessbook`) and it still returns: ```js…
[^40]: I checked the actual wire behavior, and the extension is reading it correctly
[^41]: Backfill is now best-effort: - per-session DB write failures are recorded - per-session transcript fetch/parse…
[^42]: Backfill now logs: - when it starts each host - periodic progress every 25 processed sessions - final summary …
[^43]: Cleanup performed now: - rebuilt the local `pimux` binary - restarted your local LaunchAgent
[^44]: They’re just two different host identity strings


---

# Report for 2026-04-08

# Daily report for 2026-04-08

## ~/apps/pimux2000

Worked on:
- Daily report summaries for the project
- Markdown footnote formatting for report excerpts
- Placement and presentation of footnotes in report output

Accomplished:
- Report excerpts were changed to render as Markdown footnotes[^1][^2]
- LLM-miss evidence was also updated to render as footnotes[^3]
- The report-day summary behavior was extended beyond the original command scope[^4]
- Footnotes were adjusted to appear at the bottom of the document instead of inline[^5][^6]

LLM misses:
- The report formatting still placed footnotes inline when they needed to be grouped at the bottom[^7][^8]
  Lesson: When changing markdown structure, verify global placement rules, not just rendering of individual footnotes

## ~/apps/Termsy

Worked on:
- macOS window focus behavior on new windows
- macOS raw keypress logging for input debugging
- macOS Option-key behavior and terminal byte emission
- SwiftUI app launch behavior for the settings screen
- Comparing Termsy’s macOS input handling with the official Ghostty app

Accomplished:
- Fixed the new-window focus bug by activating the app and ensuring the embedded terminal becomes key when revealed[^9]
- Added raw macOS input logging for keyDown, keyUp, and flagsChanged events in the window support layer
- Fixed a crash in the key logger by stopping it from reading character fields on flagsChanged events[^10]
- Moved the macOS settings UI to a proper Settings scene so it no longer opens as a regular window on launch[^11]

LLM misses:
- Initially attributed the macOS Option-key issue to Ghostty behavior, but the user later clarified Termsy was emitting Option-backspace on a single Option press[^12][^13]
  Lesson: When investigating input bugs, verify the app’s actual emitted bytes before assuming the upstream library or platform behavior is the cause

[^1]: can we make the excerpts markdown footnotes
[^2]: done. Changed: - `pimux-server/src/report.rs` -
[^3]: done. Changed: - `pimux-server/src/report.rs` - accomplishment excerpts now render as Markdown footnotes - `LLM
[^4]: Extended the same behavior to: - `pimux list` - `pimux agent run` - `pimux agent install
[^5]: all of the footnotes should be at the bottom, not interspersed throughout
[^6]: keep all footnotes at the document bottom -
[^7]: LLM: I ran it. The installed `pimux` on `root@pimux` did run, but it fell back to heuristic summaries there, so I r…
[^8]: Correction: all of the footnotes should be at the bottom, not interspersed throughout
[^9]: Fixed the new-window focus bug
[^10]: my logger was calling them unconditionally Fixed in: - `TermsyMac/MacWindowSupport.s…
[^11]: Fixed in `TermsyMac/TermsyMacApp.swift
[^12]: the option key is acting as backspace on macos for some reason
[^13]: to be clear, pressing "option" once deletes a word backwards, as if i was pressing option-backspace


---

