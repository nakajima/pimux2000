# Daily report for 2026-04-08

## ~/apps/pimux2000

Worked on:
- Daily report summaries for the project
- Markdown footnote formatting for report excerpts
- Placement and presentation of footnotes in report output

Accomplished:
- Report excerpts were changed to render as Markdown footnotes[^1][^2]
- LLM-miss evidence was also updated to render as footnotes[^3]
- The report-day summary behavior was extended beyond the original command scope[^4]
- Footnotes were adjusted to appear at the bottom of the document instead of inline[^5][^6]

LLM misses:
- The report formatting still placed footnotes inline when they needed to be grouped at the bottom[^7][^8]
  Lesson: When changing markdown structure, verify global placement rules, not just rendering of individual footnotes

## ~/apps/Termsy

Worked on:
- macOS window focus behavior on new windows
- macOS raw keypress logging for input debugging
- macOS Option-key behavior and terminal byte emission
- SwiftUI app launch behavior for the settings screen
- Comparing Termsy’s macOS input handling with the official Ghostty app

Accomplished:
- Fixed the new-window focus bug by activating the app and ensuring the embedded terminal becomes key when revealed[^9]
- Added raw macOS input logging for keyDown, keyUp, and flagsChanged events in the window support layer
- Fixed a crash in the key logger by stopping it from reading character fields on flagsChanged events[^10]
- Moved the macOS settings UI to a proper Settings scene so it no longer opens as a regular window on launch[^11]

LLM misses:
- Initially attributed the macOS Option-key issue to Ghostty behavior, but the user later clarified Termsy was emitting Option-backspace on a single Option press[^12][^13]
  Lesson: When investigating input bugs, verify the app’s actual emitted bytes before assuming the upstream library or platform behavior is the cause

[^1]: can we make the excerpts markdown footnotes
[^2]: done. Changed: - `pimux-server/src/report.rs` -
[^3]: done. Changed: - `pimux-server/src/report.rs` - accomplishment excerpts now render as Markdown footnotes - `LLM
[^4]: Extended the same behavior to: - `pimux list` - `pimux agent run` - `pimux agent install
[^5]: all of the footnotes should be at the bottom, not interspersed throughout
[^6]: keep all footnotes at the document bottom -
[^7]: LLM: I ran it. The installed `pimux` on `root@pimux` did run, but it fell back to heuristic summaries there, so I r…
[^8]: Correction: all of the footnotes should be at the bottom, not interspersed throughout
[^9]: Fixed the new-window focus bug
[^10]: my logger was calling them unconditionally Fixed in: - `TermsyMac/MacWindowSupport.s…
[^11]: Fixed in `TermsyMac/TermsyMacApp.swift
[^12]: the option key is acting as backspace on macos for some reason
[^13]: to be clear, pressing "option" once deletes a word backwards, as if i was pressing option-backspace
