# Daily report for 2026-04-05

## ~/apps/seekwel

Worked on:
- Comparisons and null handling in the query layer
- HasMany association design and macro/runtime support
- Planning clearer association naming and file organization
- Designing first-class FTS support
- Designing safer schema reconciliation from live DB state

Accomplished:
- Implemented `Comparison::IsNull` and `Comparison::IsNotNull` support[^1]
- Added runtime `HasMany` relation support and exported it through the model/public API[^2]
- Moved `HasMany` to a `u8` const key and had the macro generate named column-key consts for cleaner usage[^3]
- Resolved the FTS planning gaps with explicit assumptions and updated the plan[^4]
- Resolved the schema-change planning gaps with explicit assumptions and updated the plan[^5]

LLM misses:
- The HasMany design initially pushed toward a macro/type-rewriting approach when the user preferred a const-generic, nightly-friendly direction[^6]
  Lesson: When the user asks for a less invasive type-level design, validate whether a const-generic or type-state approach can satisfy the API before proposing macro rewriting
- The HasMany key was first modeled as `usize`, but the user pushed for a smaller representation[^7][^8]
  Lesson: Prefer the smallest practical internal representation when it preserves the API; re-check storage width assumptions before settling on a type

## ~/apps/Termsy

Worked on:
- Terminal input and mouse interaction behavior on iPad
- Scrolling behavior in Termsy, including smooth/inertial motion
- Session replay / tmux display artifacts
- App settings, keyboard shortcuts, and terminal state handling
- Generating an auto-created changelog/summary view from the app build

Accomplished:
- Removed the SSH-dropped warning UI from the terminal controller[^9]
- Rewrote the whats-new generation script to be pure shell by removing Python and text filtering steps[^10]

LLM misses:
- Mistook a Termsy-specific key-repeat problem for a broader system-wide iPad keyboard setting issue[^11]
  Lesson: When the user scopes a bug to the app, avoid platform-wide troubleshooting unless the app-specific path has been ruled out
- Focused on the wrong touch interaction model instead of the requested two-finger drag-to-scroll behavior[^12][^13]
  Lesson: Treat gesture requests literally and validate the exact interaction mapping before proposing implementation details
- Pursued a smooth-scrolling approach that did not match the actual tmux line-step behavior and failed to deliver the requested in-between animation[^14][^15]
  Lesson: Verify the terminal’s real scroll granularity and rendering path before designing animation around assumptions

## ~/apps/pirot

Worked on:
- Reducing convo mode eagerness and stickiness in `.pi/agent/extensions/convo.ts
- Tuning clarification detection so it asks more concrete, lower-level questions
- Adjusting the retry/follow-up flow after `agent_end` to avoid staying in convo mode too long

Accomplished:
- Identified the convo extension as the main source of over-eager entry and sticky behavior[^16][^17]
- Confirmed that the retry loop in `agent_end` was re-steering into clarification mode[^18][^19]
- Pinned down that a bare `?` should not trigger or prolong convo mode[^20][^21]

LLM misses:
- Initially treated the issue as open-ended clarification work instead of recognizing the pasted lines as the bug itself[^22]
  Lesson: When the user says a quoted line is the bug, respond by resolving the implementation issue directly rather than asking for more information

## ~/apps/pimux2000

Worked on:
- iOS app review and fixes
- Server-side review in pimux-server
- Concurrency warning cleanup
- Screenshot generation and transcript/layout issues

Accomplished:
- Applied fixes to session read-state and related app issues, then verified tests/build were passing[^23]
- Implemented concurrency-warning cleanup, including making pure helpers and data types nonisolated where appropriate[^24][^25]
- Updated compaction behavior so headless compacting re-discovers and re-summarizes the session after compaction[^26]

LLM misses:
- Screenshot work initially assumed iPad captures without the required landscape orientation[^27][^28]
  Lesson: Confirm platform-specific screenshot orientation requirements before implementing or summarizing the work

## ~/apps/hyperlinks

Worked on:
- Home page card layout and typography adjustments
- Badge placement for imported links on compact cards
- Meta line formatting for RSS and PDF badges

Accomplished:
- Reduced the home page grid to 2 columns on large screens and tightened card title wrapping to improve alignment[^29]
- Shrank the home page card typography for a more compact layout[^30]
- Added an RSS badge for feed-imported links and moved it to the meta line before the timestamp without a separator[^31][^32]
- Moved the PDF badge to the meta line on compact/home-page cards, before the timestamp, while preserving its non-compact placement[^33]


[^1]: Reworked `Comparison` from a generic enum into a concrete comparison type with: - constructors: - `Comparison:…
[^2]: ### What changed - Added `HasMany` runtime relation support in: - `src/model/relation.rs` - Exported it from: …
[^3]: ### Changed `HasMany` now uses a `u8` const key, and the macro generates named column-key consts on the `<Mode…
[^4]: Resolved the remaining gaps with explicit assumptions
[^5]: You’re right — here is the **fully closed** version with the remaining policy choices made explicit
[^6]: Correction: can't we use a const generic for HasMany<Pet, PetColumns::OwnerId> instead of rewriting with the macro
[^7]: LLM: I did some digging, and the short answer is: ### The exact nice form we want is probably **not achievable toda…
[^8]: Correction: could we go u8 instead of usize? usize seems unnecessarily large
[^9]: Agreed. I changed it so that message is never shown. What I changed: - `Termsy/Views/TerminalTabBarController.…
[^10]: ### Changed - Rewrote `ci_scripts/generate_whats_new.sh` to be **pure shell** - Removed: - Python - filtering …
[^11]: Correction: i mean only in termsy
[^12]: LLM: Hardware `UIKeyInput` callbacks are now suppressed for the whole time a physical key is held, not just the first press
[^13]: Correction: can we make two finger drags on the screen scroll instead of select text
[^14]: LLM: You were right — the first pass had a real flaw
[^15]: Correction: no downward momentum but now the whole window scrolls weirdly which is even worse
[^16]: yeah, I think your read is basically correct
[^17]: the line you pasted is basically the smoking gun
[^18]: “You still need more information...” - “Do not stop with prose.…
[^19]: agent_end` decides clarification is still needed - `questionnaireRetryCount` increments - then i…
[^20]: looksLikeClarificationRequest(...)` currently treats any `?` as a clarification…
[^21]: A bare `?` is not a real missing requirement here, and it should not reopen or prolong convo mode
[^22]: No — that pasted line is the bug itself
[^23]: All tests pass. Let me verify there were no failures
[^24]: can we fix all the concurrency warnings
[^25]: Sure. Let me walk through each warning category: --- ### Category 1: Codable types used inside a non-MainActor…
[^26]: can we re-summarize sessions on compaction
[^27]: LLM: ### Added - `pimux2000/Models/ScreenshotScenario.swift` - `pimux2000UITests/ScreenshotTests.swift` - `scripts/…
[^28]: Correction: the ipad screenshots should be landscape
[^29]: Yeah, agreed — the 3-column version was too cramped
[^30]: marks home-page cards as “compact” - `templates/hyperlinks/show.stpl` - leaves discovered-link cards at n…
[^31]: I added an `RSS` badge for feed-imported links on the home page cards
[^32]: removed `RSS` from the host row - now renders it in the meta row as: - time - status if present - `RSS` - `Det…
[^33]: on non-compact cards, `PDF` stays where it was Verified: - `cargo check` ✅ - `cargo test index_renders_p…
