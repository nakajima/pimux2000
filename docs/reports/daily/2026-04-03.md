# Daily report for 2026-04-03

## ~/apps/Termsy

Worked on:
- SSH/terminal input handling and key event routing
- Ghostty keyboard protocol behavior and terminal rendering quirks
- Autoconnect tab startup behavior and tab bar UX
- Terminal tab layout, dragging, and selection interactions

Accomplished:
- Applied a quick viewport type fix using `UInt16`[^1]
- Added logging to diagnose a silent SSH connection hang[^2]
- Implemented changes so hardware keypresses route through `ghostty_surface_key` and `insertText` is suppressed for them[^3]
- Verified the build succeeds and cleaned up stale Xcode project references after deleting a file[^4][^5]

LLM misses:
- Assumed the SSH issue was only a logging problem, but the user clarified the connection could work from other terminals and later that the app still had no terminal output[^6][^7][^8][^9]
  Lesson: Confirm whether a failure is app-specific versus environment-specific before narrowing the diagnosis
- Suggested a tab startup implementation when the user was actually asking for a more native, richer tab interaction model[^10][^11][^12]
  Lesson: Distinguish between a minimal functional answer and a request for platform-native behavior and interaction details
- Iterated on tab styling and reordering, but the user still reported poor click targets, layout shifts, and an overall bad feel[^13][^14]
  Lesson: When UX feedback is negative, validate the full interaction feel—not just the visual layout or basic reordering logic

## ~/apps/seekwel

Worked on:
- Refactoring the SQL layer toward a more modular, maintainable structure
- Clarifying the boundary between internal SQL generation and the public crate API
- Reviewing user-input handling and SQL injection risk in the query layer

Accomplished:
- Completed a first-pass SQL refactor with no behavior change, splitting `src/sql/mod.rs` into focused modules
- Made the SQL layer internal by changing `pub mod sql;` to `mod sql;`, and added a real internal statement model for `SELECT` queries[^15]


## ~/apps/pirot

Worked on:
- Planning-mode keybinding behavior in the pirot project
- Authentication flow fallback when standard auth fails

LLM misses:
- Misread the user’s intent about Shift-Tab, treating planning mode as already correctly mapped instead of responding to the correction[^16][^17]
  Lesson: When a user corrects a keybinding or behavior claim, verify the exact intended mapping before asserting the current setup

## ~/apps/pimux2000

Worked on:
- Wired the at-completions feature through the server protocol and request handling
- Added server-side plumbing for fulfillment and cleanup across the live store / IPC stack
- Implemented the iOS client-side @ completion support in the composer and session view integration

Accomplished:
- Completed the at-completions feature wiring across server and iOS; both builds and tests passed[^18]
- Server implementation covered the protocol, HTTP endpoint, agent handling, and cleanup paths for at-completions


[^1]: Viewport uses `UInt16`. Quick fix
[^2]: No SSH logs at all — the connection is hanging silently. Let me add logging to find where it stalls
[^3]: Now every hardware keypress goes through `ghostty_surface_key` only, and `insertText` is suppressed for all of them
[^4]: Build succeeds. Let me also verify the `TerminalSessionView` file reference is removed from the Xcode project …
[^5]: Clean — no stale references. Let me verify the final state of all changed files
[^6]: LLM: Try pressing ctrl+a and check the console logs for `[KEY]` output
[^7]: Correction: no output when i press ctrl+a
[^8]: LLM: The crash in `startShell` is preventing everything — the view never gets created
[^9]: Correction: no, even when it doesn't crash, there's no term
[^10]: LLM: On launch, `ContentView` queries all sessions with `autoconnect == true`, ordered by most recently connected, and opens a tab for each one
[^11]: Correction: it should support: dragging to reorder, hover preview, cmd-# to jump by index, etc
[^12]: when the app starts, we should automatically open tabs for all of the autoconnect sessions
[^13]: LLM: Stripped way back. Here's what's different: - **No `GeometryReader`** — tabs use `.frame(maxWidth: .infinity)`…
[^14]: Correction: still terrible feeling. you have to click the actual text instead of the whole tab, the layout STIL shifts, it's just bad all around
[^15]: changed `pub mod sql;` → `mod sql;` So the SQL layer is no longer part of the public crate API
[^16]: i want shift-tab to toggle planning mode instead of thinking
[^17]: It already is. Relevant files: - `.pi/agent/extensions/planning-mode.ts` — registers `shift+tab` to toggle pla…
[^18]: Both server and iOS build, tests pass
